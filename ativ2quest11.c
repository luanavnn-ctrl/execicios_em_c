#include <stdio.h>

int main(){

    int num, maior, menor, qtde_par, qtde_impar, media;

    printf("Digite um numero: ");
    scanf("%d", &num);

    while (num != 0){
        for (num )
    }




    return 0;
}